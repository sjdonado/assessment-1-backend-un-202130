name: "fibonacci Action"
description: "Fibo series Action"
author: "ronaldoc@uninorte.edu.co"
 
inputs:
  N_INDEX: 
    description: "Show fibonacci"
    required: true
    default: "Ronald"
 
runs:
  using: "docker"
  image: "Dockerfile"
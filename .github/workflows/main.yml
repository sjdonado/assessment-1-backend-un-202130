name: A workflow for mi primer parcial Backend
on: push

jobs:
  build:
    name: Sucession Fibonacci
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - uses: ./hello-world
        with:
          N_INDEX: 12

          

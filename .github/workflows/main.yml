name: A workflow for my fibonacci series
on: push

jobs:
  build:
    name: Run the World action
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - uses: ./fibo
        with:
          N_INDEX: 12

name: A workflow for my fibonacci script
on: push

jobs:
  build:
    name: first project action
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - uses: ./ejercicio
        with:
          N_VALUE: 9
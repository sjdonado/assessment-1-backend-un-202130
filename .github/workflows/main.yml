name: A workflow for my Hello World action
on: push

jobs:
  build:
    name: Run the World action
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - uses: ./fibo
        with:
          N_index: "9"
